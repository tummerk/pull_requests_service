# Результаты нагрузочного тестирования

## Входные данные

- **Количество команд**: 455 
- **Количество юзеров**: 500
- **Количество pr-ов**: 455

## Краткие выводы

Сервис успешно прошел нагрузочное тестирование. Все установленные SLI (показатели уровня обслуживания) были не просто выполнены, а превышены с большим запасом.

- **Уровень ошибок**: 0% (при требовании < 0.1%)
- **Время ответа (p95)**: 4.42 мс (при требовании < 300 мс)
- **Выдерживаемая нагрузка**: ~60 RPS (при требовании 5 RPS)

## Параметры тестирования

| Параметр                  | Значение                                                                                                                                                                      |
|---------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Инструмент**            | Grafana k6                                                                                                                                                                    |
| **Продолжительность**     | ~2 минуты                                                                                                                                                                     |
| **Профиль нагрузки**      | Плавное увеличение с 0 до 50 виртуальных пользователей, удержание пиковой нагрузки в течение 1 минуты и плавное снижение                                                      |
| **Тестируемые эндпоинты** | <ul><li>GET /team/get (получение информации о команде)</li><li>GET /users/getReview (получение списка PR для ревью)</li><li>GET /stats (получение общей статистики)</li></ul> |

## Результаты и соответствие требованиям (SLI/SLO)

| Требование (SLI)             | Целевой показатель (SLO) | Фактический результат |
|------------------------------|--------------------------|-----------------------|
| Успешность запросов          | > 99.9%                  | 100% (rate=0.00%)     |
| Время ответа                 | p(95) < 300 мс           | p(95) = 4.42 мс       |
| Выдерживаемая нагрузка (RPS) | > 5 RPS                  | ~59 RPS               |

Как видно из результатов, сервис продемонстрировал абсолютную стабильность (0 ошибок) и высокую производительность, превысив требования по времени ответа более чем в 60 раз.

## Детализация по эндпоинтам

Анализ кастомных метрик времени ответа для каждого эндпоинта показывает, что все операции чтения выполняются очень быстро:

| Эндпоинт             | Среднее время | 95-й перцентиль (p95) |
|----------------------|---------------|-----------------------|
| GET /stats           | 3.59 мс       | 5.07 мс               |
| GET /team/get        | 2.11 мс       | 3.02 мс               |
| GET /users/getReview | 1.78 мс       | 2.82 мс               |
